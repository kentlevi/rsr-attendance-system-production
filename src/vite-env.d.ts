/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Bundle version baked at build time via vite define (vite.config.js). */
declare const __APP_VERSION__: string;
